# manuscript — agent notes

Manuscript variables are injected at render time by the external template checkout; never hardcode a number or digest the code can produce. The formalism file carries `::: {#def:...}` / `::: {#prop:...}` blocks.
