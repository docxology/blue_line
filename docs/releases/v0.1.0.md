# v0.1.0 — 2026-08-29

Initial release.

Gates at cut time (all run from a clean `uv sync`):

- `uv run pytest tests/ --cov=src --cov-report=term`: 87 passed, 1 skipped
  (sibling-schema test; sibling absent here), coverage 97.33% (floor 90).
- `uv run ruff check src tests scripts`: clean.
- `uv run python scripts/build_figures.py`: 3 figures + manifest, byte-stable.
- `uv run python scripts/check_registry.py`: invariants battery all PASS.
- `uv run pytest tests/test_formalism_claim_ledger.py`: ledger matches the
  live package and its digest verifies.
