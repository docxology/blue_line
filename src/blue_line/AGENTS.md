# src/blue_line — agent notes

Runtime package. Pure standard library; frozen dataclasses; module
docstrings bound what each module does and does not establish. Sort before
emit; fail closed; no mocks; derive every number.
