"""Version markers for the Blue Line package.

The package version tracks the code surface; the registry digest (see
``serialization.registry_digest``) tracks the commitment content. Reviewers
can pin both when citing a stewardship reading.
"""

__version__ = "0.1.0"
