# tests — agent notes

Real records and temp dirs only; the no-mocks test scans this tree. Run one
directory per invocation is unnecessary here (no conftest name collisions
with siblings — this repo is standalone).
